import { Component, ViewEncapsulation } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-media-mva-erklaringer',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './media-mva-erklaringer.component.html',
  encapsulation: ViewEncapsulation.None
})
export class MediaMvaErklaringerComponent {}
